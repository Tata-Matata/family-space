package server

/* db, err := sqlite.Open("auth.db")
if err != nil {
    log.Fatal(err)
}

registrationService := service.NewRegistrationService(
    db,
    //function reference
    sqlite.NewUserStore,
) */
